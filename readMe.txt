Problèmes rencontrés:

Soucis d’autorisation d'accès aux logs Laravel (résolu avec chmod 777);
Mémoire vive allouée insufisante;
Synchronisation fichier partagé Vagrant (le fichier hôte reste vide, la création d'un test.html dans ce dernier supprime l'ensemble du projet sur la VM);

